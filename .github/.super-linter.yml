# Super-Linter configuration
# Allows tabs and CRLF line endings while catching real issues

rules:
  indentation:
    spaces: consistent

disable:
  - JSCPD  # Disable code duplication checking
  - NATURAL_LANGUAGE  # Disable prose linting

fix: false  # Don't auto-fix; report only

